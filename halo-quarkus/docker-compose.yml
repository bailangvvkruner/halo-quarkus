version: '3.8'

services:
  halo:
    build: .
    container_name: halo-quarkus
    ports:
      - "8090:8090"
    environment:
      HALO_WORK_DIR: /root/.halo2
    volumes:
      - halo-data:/root/.halo2
    restart: unless-stopped

volumes:
  halo-data:
